{% extends 'base.html.twig' %}

{% block title %}
Commande
{% endblock %}

{% block body %}

<div class="row">

        {# si l'utilisateur n'a pas encore d'adresse #}

        {# formulaire d'adresse #}
        
<div class="col">
        <h1>Adresse de facturation</h1>
           {{form(formInfo)}}    
          
              
</div>

</div>

{% if submit == 1 %}
<a href="{{ path('infolivraison', { step: 1})}}"<button type="button" class="btn btn-primary btn-lg">Suivant</button></a>
{% endif %}


{% endblock %}